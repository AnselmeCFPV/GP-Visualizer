{
  "name": "prenois",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @prenois/app",
    "build": "npm run build -w @prenois/app",
    "preview": "npm run preview -w @prenois/app"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
